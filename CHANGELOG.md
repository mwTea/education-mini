# 版本记录（CHANGELOG）

追踪每一次对线上有影响的变更：小程序端（前端）与后端服务分开记录版本与上线状态。

## 维护约定

- **何时记**：每次推送 `main`、部署后端、或上传小程序新版后，追加一条记录；只改本地未上线的改动不单独立条，合入下一条。
- **记什么**：版本号 / 日期 / 提交哈希 / 变更范围（前端、后端、双端）/ 上线状态 / 验证状态。
- **版本号**：前端小程序以开发者工具上传时填写的版本号为准（建议 `v2.0.0` 起按 semver）；后端无独立发版，按"已部署的 main 提交"记录。
- **上线操作**：
  - 后端：`bash deploy/deploy.sh [文件...]`（不带参数全量；自动 rsync → 远端语法校验 → `systemctl restart copybook` → 健康检查）
  - 代码推送：`bash deploy/git-push.sh "提交说明"`（当前分支 → `origin/main`，走本地代理）
  - 小程序：微信开发者工具上传（版本号与本次记录一致）
- 详细开发过程见 `docs/v2/planning/Sprint0-工作档案.md`；V2 资料索引见 `docs/v2/README.md`。

## 版本总览

| 版本 | 日期 | 提交 | 范围 | 前端上线 | 后端上线 | 主题 |
| --- | --- | --- | --- | --- | --- | --- |
| v1.0 | 2026-09-11 | `5426e88` | 双端 | 已发布（线上版） | 已部署 | 字帖出题器首版 |
| v1.0.1 | 2026-09-11 | `1506619` | 前端 | 随 v1.0 线上版 | — | 首页恢复原版设计 |
| v2.0.0-a1 | 2026-09-11 | `2df5c03` | 前端 | 未上传 | — | V2 五导航 UI 框架 |
| v2.0.0-a2 | 2026-09-11 | `2ab2eff` | 前端 | 未上传 | — | UI 换皮（令牌/小墨/图标/清扫） |
| be-0911.1 | 2026-09-11 | `dfb25ed` | 后端 | — | 已部署 | 教材拼音接口 |
| v2.0.0-a3 | 2026-09-11 | `7294d8c` | 前端 | 未上传 | — | 在线练习完善（三科/交卷/续练/成长） |

> 当前线上后端 = `main@7294d8c` 的 `backend/`（2026-09-11 全量部署并回归验证）。前端 v2 各 alpha 版尚未上传小程序，以开发者工具实际上传为准。

---

## v2.0.0-a3（2026-09-11，`7294d8c`）在线练习体验完善

**范围**：前端（含 `utils/learning.js`）

### 新增
- 语文·看拼音选字（`pages/chinese/quiz`）：按年级/学期选课，每课至多 10 题四选一，干扰项取全册生字；结算含错字回顾。
- 英语·记词拼单词（`pages/english/quiz`）：词库横选 + 单元勾选；3 秒记词后用打乱字母按序拼出，整轮 3 次"看一眼"提示。
- 交卷：三个在线练习（含口算）答题中可提前交卷，按已答题数结算正确率。
- 续练：进度自动保存（每题推进 + 退出页面），再次进入弹窗可"继续上次练习"；交卷后进度清除。存储键：`cb_{math,zh,en}_quiz_progress`。

### 优化
- 成长页接入三科练习记录（`learning.logRun` + `subjectSummary`），学习天数/完成题数/正确率含三科；旧口算数据自动兼容。
- 学习页：语文/英语升级为与数学同级的大学科卡（学科浅底 + 小墨陪练 + 学科色按钮）。
- 首页快捷四宫格按功能权重重排：在线口算 / 在线英语 / 语文打印 / 打印历史；新增 Abc、田字格两枚图标。
- 名字字帖入口从首页移入打印页工具格（替换"专项训练"占位）；首页清理孤立方法。

### 验证
- 后端 44 项测试全过；JS 语法、16 页四件套、资源引用、事件绑定自检通过。
- 待开发者工具/真机视觉验收后上传。

---

## be-0911.1（2026-09-11，`dfb25ed`）教材拼音接口

**范围**：后端（已部署线上 `/opt/copybook-backend`）

- `GET /api/v1/textbooks/:id` 每课新增 `items:[{ch, py}]` 逐字注音（pinyin-pro，多音字取常用读音，与打印注音同源）。
- 纯增量：原有 `chars`/`title` 等字段全部保留，字帖生成、打印、口算不受影响。
- 线上回归：健康检查、词本、口算出题、字帖生成、PDF 下载全部通过（测试字帖已删除）。
- 另：`deploy/deploy.sh` 修复 ssh 参数拼接导致的认证失败（`IdentitiesOnly + BatchMode`）。

---

## v2.0.0-a2（2026-09-11，`2ab2eff`）UI 换皮

**范围**：前端

### 新增
- `app.wxss` 设计令牌：品牌蓝、学科色（语红/数蓝/英绿）、氛围橙、奶油纸底、圆角/投影。
- 小墨素材：经授权从 Design System 总览图裁切 8 个状态透明 PNG（`assets/mascot/`，源约 65–80px，中小尺寸可用，官方切图可同名替换）。
- TabBar 五图标（灰/蓝两态）与快捷入口图形图标（`scripts/gen_icons.py` 生成）。

### 优化
- 首页暖橙渐变 Hero + 小墨 + 三科渐变图标卡；学习/打印/成长/我的统一令牌；`profile` 清理死样式。
- 内页清扫（`scripts/remap_inner_colors.py`）：9 个 wxss 的 v1 暖橙纸感色映射到令牌，清扫后零残留。

---

## v2.0.0-a1（2026-09-11，`2df5c03`）V2 五导航 UI 框架

**范围**：前端

- 底部导航调整为：首页、学习、打印、成长、我的。
- 新增学习/打印/成长三个一级页面；打印历史迁为普通页面。
- 全局主色由暖橙纸张风格调整为清爽蓝色体系；不动出题、PDF、登录与持久化逻辑。

---

## v1.0.1（2026-09-11，`1506619`）首页恢复原版设计

- Hero 渐变 + 三科全宽卡 + 快捷四宫格；移除"保存常用练习"。

## v1.0（2026-09-11，`5426e88`）字帖出题器首版

- 语数英三科同步练习：生字/词语/字帖生成、单词/默写、口算题卡、偏旁部首、打印历史、PDF 下载。
- 后端：Node.js（Express），教材/词本/古诗词数据内置，字帖 JSON 持久化，HTTPS 域名 `edu.10010rights.com.cn`。
