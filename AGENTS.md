# 描红习字簿 V2 开发说明

## 当前项目
- 前端位于 `miniprogram/`，使用原生微信小程序。
- 后端位于 `backend/`，使用 Node.js；数据库与持久化方式以实际代码审计为准。
- 优先复用现有实现，不允许为 V2 推倒重写已有稳定功能。

## V2 资料入口与状态
阅读 `docs/v2/README.md`。已归档用户提供的四份 v1.0 文档与九张原图。文档在 `docs/v2/design/` 和 `docs/v2/planning/`，视觉资料在 `docs/v2/ui/`，重复副本在 `docs/v2/archive/duplicates/`。产品/功能 v1.1 与独立小墨切图未提供，具体版本差异和图片使用边界见索引。
文档冲突优先级为：Sprint 开发任务书 > 技术详细设计 > 功能详细设计 > 产品迭代设计。发现与现有实现冲突时先说明并适配，不机械新增表或接口。

## 产品原则
- 按实际使用率、开发成本、维护成本判断功能，不要过度设计。
- 打印可以深，在线练习先轻；底层可以专业，前台交互必须简单。
- 不自行增加设计文档之外的功能。
- 视觉只采用原版小墨与原有 UI 体系；后续偏高年级、偏简洁的备选方案已放弃，不混用两套设计。
- 打印 PDF 内页模板待用户提供成熟参考后另行确定。

## 开发流程
每次只开发一个 Sprint。当前阶段为 Sprint 1 开发前准备，先按实际归档的 Sprint 任务书开展审计；此次仅整理资料，不开始实现。
开发前先扫描现有代码，审计登录/用户、持久化、孩子/年级/教材、首页与我的、在线口算、题库、PDF/打印及接口组织。
列出可复用、需改造、必须新增内容，数据库/API/页面变化、预计修改文件、冲突与实施顺序；给出实施方案，等方案确认后再进行大范围修改。
本次整理任务不授权修改业务代码。
