# Sprint 0 工作档案：V2 UI 换壳

## 基本信息

- 分支：`codex/v2-sprint-00-ui-shell`
- 启动日期：2026-09-11
- 状态：代码骨架完成，待微信开发者工具视觉验收
- 目标：在不改动核心出题、PDF、登录和持久化逻辑的前提下，将现有功能接入 V2 UI 框架。

## 已确认范围

- 底部导航调整为：首页、学习、打印、成长、我的。
- 首页使用 V2 蓝色主调，语文、数学、英语三科等权展示。
- 学习页承接现有语文、数学、英语入口；当前可在线练习的数学口算直接复用。
- 打印页承接现有三科生成、偏旁部首和打印历史。
- 成长页只读取设备内已有口算汇总；未实现的语文、英语统计和日历明确标记待开放。
- 我的页面保留当前本地年级、配额、历史、帮助和反馈入口。
- 多孩子切换暂缓；数据库、接口、出题算法和 PDF 内页不在本 Sprint 修改。

## 实施记录

### 2026-09-11

- 从 `main@1506619` 创建分支 `codex/v2-sprint-00-ui-shell`。
- 工作区启动时已有 V2 规划文档变更，继续保留并纳入工作档案。
- 新增学习、打印、成长三个一级页面，组成五导航骨架。
- 首页切换为 V2 品牌、三科等权卡片和四个常用入口。
- 打印历史从旧 Tab 迁为普通页面入口，修复首页和“我的”跳转方式。
- 全局主色由暖橙纸张风格调整为设计稿的清爽蓝色体系。
- 未使用设计总览图裁切角色；独立小墨素材缺失，首页暂用纯 CSS 练习册图形占位。

## 变更文件

- `miniprogram/app.json`
- `miniprogram/app.wxss`
- `miniprogram/pages/index/index.{js,wxml,wxss}`
- `miniprogram/pages/profile/profile.{js,wxml,wxss}`
- `miniprogram/pages/learn/*`
- `miniprogram/pages/print/*`
- `miniprogram/pages/growth/*`
- `docs/v2/README.md`
- `docs/v2/planning/实施审计与排期建议.md`
- `docs/v2/planning/Sprint0-工作档案.md`

## 设计决策

- 现有三科学科配置页继续同时包含在线与打印能力；Sprint 0 只从新的学习/打印入口路由过去，不复制业务页面。
- 成长页展示的数据来源和范围写明为设备内口算记录，避免把占位内容表达成真实服务端统计。
- 专项训练、测试小卷等入口保留视觉位置，点击仅提示后续开放。
- 原版小墨没有独立透明切图前不混用替代角色，也不从总览图直接裁切低质量素材。

## 验证记录

### 2026-09-11 自动检查

- `app.json`及全部小程序JSON配置解析通过。
- `app.json`登记的14个页面均具备JS、JSON、WXML、WXSS文件。
- 本次新增和修改的页面JavaScript通过`node --check`。
- `git diff --check`通过，无空白错误。
- 后端`npm test`通过：44项通过，0失败。沙箱内首次运行因测试监听临时端口受限，8项API用例被取消；获准在沙箱外复跑后全部通过。

待完成：微信开发者工具编译、五Tab点击回归、不同机型视觉检查和真机验收。当前环境未执行这些检查，因此不把Sprint 0标记为最终验收完成。

## 遗留与下一步

- 独立小墨素材到位后替换 CSS 占位图形。
- 根据微信开发者工具截图微调不同机型的字号、留白和 Tab 表现。
- Sprint 1 再实现单孩子档案、教材上下文和持久化升级。

---

# 续：UI 换皮（分支 `codex/v2-ui-reskin`）

## 基本信息

- 分支：`codex/v2-ui-reskin`，基于本档案上述骨架（`2df5c03`）。
- 日期：2026-09-11。
- 目标：按效果图完成视觉换皮——设计令牌、图形图标、小墨进场、内页旧色清扫；不改布局与业务逻辑。

## 用户确认的关键决策

- 小墨素材采用方案 b：授权从 Design System 总览图裁切单状态试用，先验清晰度。裁切结果与边界说明见 `docs/v2/ui/mascot/README.md`。
- 首页 Hero 采用全流程参考图（10_30_56）的暖橙渐变，全局主操作与选中态保持品牌蓝：蓝做体系、橙做氛围。

## 实施记录

### 视觉令牌与基础

- `app.wxss` 新增 CSS 变量令牌：品牌蓝、学科色（语红/数蓝/英绿）、氛围橙、暖纸底 `#f7f5f0`、圆角与投影；页面底色由冷灰蓝改为奶油白。
- 清理两处 v1 遗迹：`.btn-primary` 的"暖橙渐变"过期注释、`.seg-item.on` 的棕调阴影。

### 图标与素材

- 新增 `scripts/gen_icons.py`（PIL 8 倍超采样）生成：TabBar 五图标 × 灰/蓝两态（81×81，全部 <40KB）、快捷入口四彩色图形（铅笔/名牌/四宫格/时钟），存于 `assets/tabbar/`、`assets/quick/`；`app.json` TabBar 接入图标。
- 新增 `scripts/` 下小墨裁切流程：程序化网格定位 + 泛洪去底 + 边缘柔化，产出 8 个状态透明 PNG 到 `assets/mascot/`（源单状态约 65–80px，中小尺寸可用，大尺寸偏软；官方切图可同名替换）。

### 页面换皮

- 首页：暖橙渐变 Hero + 小墨 wave + 白色主按钮（深橙文字）；三科图标升级为学科渐变圆角块；快捷四宫格换图形图标；卡片统一令牌投影，去描边。
- 学习/打印/成长/我的：统一令牌；学科图标渐变化；成长空态与"我的"头像接入小墨（read/wave）；`profile.wxss` 清理 wxml 已不引用的死样式（mi-chip 等）。
- 内页清扫（`scripts/remap_inner_colors.py`，最长字面量优先替换）：语文/偏旁→学科红，英语→学科绿，数学三页与历史/预览→品牌蓝，关于→氛围橙；纸感底/棕字/暖线统一映射中性令牌。田字格绿、示例字红等功能色保留。清扫后全仓 wxss 无 v1 暖橙残留（脚本校验）。

## 验证记录

- 全部 JSON 配置解析通过；app.json 登记页面文件齐全。
- wxml 引用的静态资源全部存在；TabBar 图标 81×81 且均 <40KB。
- 全部 `var(--token)` 引用与 `app.wxss` 定义比对：无未定义令牌。
- `git diff --check` 通过；本次未改动任何 JS 与业务逻辑。
- 待办：微信开发者工具编译、五 Tab 与各内页真机/模拟器视觉验收（本环境无法执行）。

## 第二轮调整（2026-09-11，用户反馈后，待验证未提交）

用户反馈三点：学习/打印功能重复、首页“开始练习”直达口算易懵、与效果图仍有差距；并要求验证后再提交。据此调整：

- 学习页改为“在线练习”专区：数学口算主卡（小墨陪写）+ 快速续练 + 语文/英语“待开放”占位行；不再放三科学科配置入口，与打印页职责分离（打印页继续承接三科资料生成与工具）。
- 首页 Hero 按钮改为跳转学习专区（`goLearn`，switchTab），消除“开始练习”直达口算的困惑；快捷宫格“在线口算”保留直达。
- 首页视觉向效果图靠拢：Hero 加高至 300rpx、36rpx 大圆角、三段暖橙渐变、小墨放大至 220rpx，新增气泡/星形/圆环装饰；三科卡改为学科浅色渐变底 + 渐变图标 + 箭头浅章。
- 补漏：`index.json` 导航栏 v1 纸色与 `app.json` 窗口冷灰蓝底统一为奶油纸底 `#f7f5f0`。
- 本轮 JS 仅新增 `goLearn` 与学习页跳转方法，无业务逻辑改动；验证通过前不提交。

## 第三轮微调（2026-09-11，用户反馈后，待验证未提交）

- 首页右上角改为 v1 印章样式「学」（红底白字、楷体、旋转 8°），替换原橙色渐变「习」块。
- 快捷四宫格：先试浅彩底卡（与三科浅彩卡同屏显乱）、再试单张白卡（用户反馈更丑），最终按效果图像素采样结论定为**无容器直排**——彩色图形直接排在页面底上、标签在下、图标放大至 92rpx 加轻投影，与参考图一致。
- 「我的」页：意见反馈按钮补 font-weight/line-height/font-family 归一（消除原生 button 字体差异）；年级选择重排为"标签在上 + 六档通栏一行"，选中态品牌蓝渐变。
- 同步调研语文/英语最低成本在线功能选型（结论见对话，未实施）。

## 第四轮：首批语英在线练习（2026-09-11，用户指定，待验证未提交）

- 首页快捷四宫格经三轮迭代（浅彩卡→白卡合体→无容器直排）后，用户选定回退浅彩底卡版本。
- 后端：`GET /api/v1/textbooks/:id` 的 lessons 新增逐字注音 items（pinyin-pro，多音字取常用读音，与打印注音同源）；api.test.js 增加断言，44 项测试全过。**需重新部署后端后线上才可用**。
- 新增 `pages/chinese/quiz`：看拼音选字。按年级/学期选课（默认取我的页年级），每课至多 10 题四选一，干扰项从全册生字抽取；结算含星级与错字回顾；结果存 `cb_zh_quiz_last`。
- 新增 `pages/english/quiz`：记词拼单词。词库横选（默认 pep-{年级}a）+ 单元勾选；每词先 3 秒记词，再用打乱字母按序拼出，错点抖动，整轮 3 次"看一眼"提示；结算统计无提示拼对/提示/错点；结果存 `cb_en_quiz_last`。
- 学习页两个"待开放"占位替换为上述两个真实入口；app.json 注册两个新页面（共 16 页）。
- 成长页本期未接入语英记录（继续只读口算），留待统一练习版本。

## 部署与验证注意事项

- 用户本地 `miniprogram/config/index.js` 已自行改为强制线上地址；语文练习依赖后端新增拼音字段，验证前需本地起后端并指回本地地址，或先部署后端。

## 第五轮调整（2026-09-11，用户反馈后，待验证未提交）

- 学习页语文/英语入口升级为与数学同级的大学科卡：学科浅底渐变 + 小墨（数学 write/语文 read/英语 star）+ 学科色"去练习"按钮；原"更多学科"列表行废弃。
- 语文"点击开始无反应"定位：线上后端未部署拼音接口，lessons 无 items，页面静默返回。已改为：生字数回退按 chars 统计、顶部警示条 + 点击弹窗说明（启动本地最新 backend 或部署后再试），不再无声失败。
- 首页快捷四宫格按功能权重重排：在线口算（蓝铅笔）/ 在线英语（绿 Abc）/ 语文打印（红田字格）/ 打印历史（紫时钟）；新增两枚图标并清理旧图标。
- 名字字帖入口从首页移入打印页工具格（替换"专项训练"占位），偏旁部首仍保留在打印页与"我的"；首页清理孤立 JS 方法。

## 第六轮：线上部署 + 成长接入 + 交卷续练（2026-09-11，待验证未提交）

### 后端已部署线上

- 用用户提供的 `deploy/deploy.sh` 部署教材拼音接口两个文件到 `/opt/copybook-backend`，`systemctl restart copybook` 后健康检查 200。
- 脚本原 rsync/ssh 引号拼接有误导致认证失败，已修（`-o IdentitiesOnly -o BatchMode`，密钥带 macOS 隔离标记）。
- 线上验证：`GET /api/v1/textbooks/rj-yuwen-1a` 已返回逐字拼音，原有 `chars` 字段保留。
- 后端改动已单独提交（dfb25ed）；GitHub 推送因本机网络不通失败，仅存本地。

### 成长页接入三科

- `utils/learning.js`：`record()` 记流水时补 `subject:'math'`；新增 `logRun(subject,cfg,count,correct,seconds)`（语/英记账，不进错题本）与 `subjectSummary()`（分学科近 7 天汇总，旧数据无 subject 归入口算）。
- 语文/英语练习 `finish()` 时记账；成长页三科行展示真实数据，"待开放"标识移除，汇总卡含三科。
- 此前"练了没数据"的根因：口算只有整局打完才记账（中途退出不记）；语/英新游戏原先未接记账。

### 交卷与续练（三个在线练习一致）

- 答题页右上新增"交卷"：确认弹窗后按已答题数结算（正确率按已答计算），至少答一题才可交卷。
- 进度自动保存（每题推进 + onHide），再次进入弹窗询问"继续上次练习？"，可继续或重新开始；正常结算后进度清除。
- 存储键：`cb_math_quiz_progress` / `cb_zh_quiz_progress` / `cb_en_quiz_progress`；英语续练从当前词的记词阶段重新开始。

